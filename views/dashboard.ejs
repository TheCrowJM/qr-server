<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Panel - QR Manager Pro</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="<%= darkMode ? 'dark' : '' %>">
  <header class="dashboard-header">
    <h1>📊 Dashboard</h1>

    <!-- ✅ Botón Dark Mode arriba a la derecha -->
    <form action="/toggle-darkmode" method="post" class="dark-toggle-form top-right">
      <button type="submit" class="dark-toggle-btn">
        <%= darkMode ? '🌙 Modo Claro' : '🌞 Modo Oscuro' %>
      </button>
    </form>

    <nav class="menu">
      <div class="menu-items">
        <span class="user">👤 <%= currentUser %></span>
        <a href="/change-password">Cambiar contraseña</a>
        <form action="/logout" method="get" class="inline-form">
          <button type="submit" class="logout-btn blue-btn">Cerrar sesión</button>
        </form>
        <form action="/delete-account" method="post" class="inline-form">
          <button type="submit" class="delete-account-btn" onclick="return confirm('¿Estás seguro de eliminar tu cuenta? Esta acción no se puede deshacer.')">
            Eliminar cuenta
          </button>
        </form>
      </div>
    </nav>
  </header>

  <main class="dashboard-main">
    <section class="generate-section">
      <h2>Crear nuevo QR</h2>
      <form action="/generate" method="post" class="generate-form">
        <input type="text" name="originalUrl" placeholder="https://example.com" required />
        <button type="submit" class="blue-btn">Generar QR</button>
      </form>
    </section>

    <section class="qrs-section">
      <h2>📱 Tus códigos QR</h2>
      <% if (qrs.length === 0) { %>
        <p class="empty">Aún no has generado ningún QR.</p>
      <% } else { %>
        <div class="qr-grid">
          <% qrs.forEach(qr => { %>
            <div class="qr-card">
              <img src="<%= qr.qrDataUrl %>" alt="QR Code" class="qr-image" />
              <div class="qr-info">
                <p><strong>URL destino:</strong> <a href="<%= qr.originalUrl %>" target="_blank"><%= qr.originalUrl %></a></p>
                <p><strong>URL intermedia:</strong> <a href="<%= qr.shortInternalUrl %>" target="_blank"><%= qr.shortInternalUrl %></a></p>
                <p><strong>Último escaneo:</strong> <%= qr.lastScan ? new Date(qr.lastScan).toLocaleString("es-VE", { timeZone: "America/Caracas" }).replace(',', ' -') : "Nunca" %></p>
                <p><strong>Total escaneos:</strong> <%= qr.scans %></p>
              </div>

              <form action="/update/<%= qr.id %>" method="post" class="update-form">
                <input type="text" name="originalUrl" placeholder="Nueva URL de destino" required />
                <button type="submit" class="blue-btn">Actualizar</button>
              </form>

              <form action="/delete/<%= qr.id %>" method="post" class="delete-form">
                <button type="submit" class="delete-btn" onclick="return confirm('¿Eliminar este QR?')">Eliminar</button>
              </form>
            </div>
          <% }) %>
        </div>
      <% } %>
    </section>
  </main>
</body>
</html>
