<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>QR Manager Pro</title>
</head>
<body>
  <div class="brand">QR Manager Pro</div>
  <div class="controls">
    <span class="user">Usuario: <strong><%= currentUser || 'Invitado' %></strong></span>
    <a href="/change-password">Cambiar contraseña</a>
    <form action="/logout" method="POST" style="display:inline;">
      <button type="submit">Cerrar sesión</button>
    </form>
  </div>

  <h2>QRs recientes</h2>
  <ul>
    <% qrs.forEach(qr => { %>
      <li><%= qr.originalUrl %> - Scans: <%= qr.scans %></li>
    <% }) %>
  </ul>
</body>
</html>
