<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Registrarte - QR Manager Pro</title>
  <link rel="stylesheet" href="/css/style.css" />
  <script defer src="/js/dark-mode.js"></script>

  <style>
    /* 🛠️ Campos amplios y redondeados */
    input[type="text"],
    input[type="password"],
    input[name="username"],
    input[name="password"],
    input[name="passwordConfirm"] {
      width: 100%;
      padding: 12px 14px;
      margin-top: 6px;
      border: 1px solid var(--border);
      border-radius: 10px;
      font-size: 1rem;
      box-sizing: border-box;
    }

    input:focus {
      outline: none;
      border-color: var(--accent);
      box-shadow: 0 0 4px var(--accent);
    }

    label {
      font-weight: 600;
      margin-top: 12px;
      display: block;
    }
  </style>
</head>
<body class="<%= darkMode ? 'dark-mode' : '' %>">
  <div class="container">
    <div class="card">
      <div class="header">
        <div class="brand">QR Manager Pro</div>
        <div class="controls">
          <button class="btn small" data-toggle-dark>
            <%= darkMode ? 'Light Mode' : 'Dark Mode' %>
          </button>
        </div>
      </div>

      <h1>Crear cuenta</h1>
      <% if (typeof error !== 'undefined' && error) { %>
        <p style="color:var(--danger)"><%= error %></p>
      <% } %>

      <form action="/register" method="POST" style="margin-top:8px;">
        <label>Usuario</label>
        <input name="username" required placeholder="usuario" />

        <label>Contraseña</label>
        <input name="password" type="password" required placeholder="contraseña" />

        <label>Confirmar contraseña</label>
        <input name="passwordConfirm" type="password" required placeholder="repite la contraseña" />

        <div style="display:flex; gap:8px; margin-top:12px;">
          <button class="btn" type="submit">Registrarte</button>
          <a href="/" class="link" style="align-self:center">Volver</a>
        </div>
      </form>

      <p style="margin-top:18px" class="note">
        ¿Ya tienes cuenta?
        <a href="/login" style="color:var(--accent); text-decoration:underline">Iniciar sesión</a>
      </p>
    </div>
  </div>

  <script>
    // 🧠 Verificación simple de contraseñas
    const form = document.querySelector('form[action="/register"]');
    if (form) {
      form.addEventListener('submit', (e) => {
        const p = form.querySelector('input[name="password"]').value;
        const pc = form.querySelector('input[name="passwordConfirm"]').value;
        if (p !== pc) {
          e.preventDefault();
          alert('Las contraseñas no coinciden');
        }
      });
    }

    // 🧠 Autocompletar https:// si luego hay campos tipo URL
    document.querySelectorAll('input[type="url"]').forEach(input => {
      input.addEventListener('blur', () => {
        let val = input.value.trim();
        if (val && !/^https?:\/\//i.test(val)) {
          input.value = 'https://' + val;
        }
      });
    });
  </script>
</body>
</html>
