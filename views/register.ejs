<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Registrarte - QR Manager Pro</title>
  <link rel="stylesheet" href="/css/style.css" />
  <script defer src="/js/dark-mode.js"></script>
</head>
<body class="<%= darkMode ? 'dark-mode' : '' %>">
  <div class="container">
    <div class="card">

      <div class="header">
        <div class="brand">QR Manager Pro</div>
        <button class="btn small" data-toggle-dark>
          <%= darkMode ? 'Light Mode' : 'Dark Mode' %>
        </button>
      </div>

      <h1>Crear cuenta</h1>
      <% if (typeof error !== 'undefined' && error) { %>
        <p style="color:var(--danger)"><%= error %></p>
      <% } %>

      <form action="/register" method="POST">
        <label>Usuario</label>
        <input type="text" name="username" required placeholder="usuario" />
        <label>Contraseña</label>
        <input type="password" name="password" required placeholder="contraseña" />
        <label>Confirmar contraseña</label>
        <input type="password" name="passwordConfirm" required placeholder="repite la contraseña" />

        <div class="form-buttons">
          <button class="btn" type="submit">Registrarse</button>
          <a href="/" class="btn small" style="background:var(--accent-dark)">Volver</a>
        </div>
      </form>

      <p class="note">¿Ya tienes cuenta? <a href="/login">Inicia sesión aquí</a></p>
    </div>
  </div>

  <script>
    const form = document.querySelector('form[action="/register"]');
    if (form) {
      form.addEventListener('submit', (e) => {
        const p = form.querySelector('input[name="password"]').value;
        const pc = form.querySelector('input[name="passwordConfirm"]').value;
        if (p !== pc) {
          e.preventDefault();
          alert('Las contraseñas no coinciden');
        }
      });
    }
  </script>
</body>
</html>
