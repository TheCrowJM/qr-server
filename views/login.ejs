<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Iniciar sesión - QR Manager Pro</title>
  <link rel="stylesheet" href="/css/style.css" />
  <script defer src="/js/dark-mode.js"></script>
</head>
<body class="<%= darkMode ? 'dark-mode' : '' %>">
  <div class="container">
    <div class="card">
      <div class="header">
        <div class="brand">QR Manager Pro</div>
        <div class="controls">
          <button class="btn small" data-toggle-dark aria-label="Toggle theme"><%= darkMode ? 'Light Mode' : 'Dark Mode' %></button>
        </div>
      </div>

      <h1 style="margin-top:6px">Login</h1>
      <% if (typeof error !== 'undefined' && error) { %>
        <p style="color:var(--danger)"><%= error %></p>
      <% } %>

      <form action="/login" method="POST" style="margin-top:8px;">
        <label>Usuario</label>
        <input name="username" required placeholder="usuario" autocomplete="username" />

        <label>Contraseña</label>
        <input name="password" type="password" required placeholder="contraseña" autocomplete="current-password" />

        <div style="display:flex; gap:8px; margin-top:12px; justify-content:center;">
          <button class="btn" type="submit">Iniciar sesión</button>
        </div>

        <p style="margin-top:14px; text-align:center;" class="note">
          ¿No tienes cuenta?
          <a href="/register">Regístrate</a>
        </p>

      </form>
    </div>
  </div>
</body>
</html>
